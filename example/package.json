{
  "name": "@jofr/capacitor-media-session-example-app",
  "version": "1.0.0",
  "description": "Media Session Example App",
  "main": "index.js",
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "sync": "npm run build && cap sync",
    "build:android": "npm run build && cap sync android && cd android && ./gradlew assembleDebug",
    "test:android": "npm run build && cap sync android && cd android && ./gradlew testDebugUnitTest"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@jofr/capacitor-media-session": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4",
    "jsdom": "^25.0.1",
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  },
  "author": "",
  "license": "ISC"
}
